plugins {
  id "idea"
  id "java-library"
  id "org.jetbrains.gradle.plugin.idea-ext" version "1.1.3"
}

allprojects {
  idea {
    module {
      outputDir file("build")
      sourceDirs += files("src")
      excludeDirs += files(".cache", ".gradle", "gradle", ".yarn", "build", ".tmp")
    }
  }
}