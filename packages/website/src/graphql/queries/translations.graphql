query translations($locale: I18NLocaleCode = "ru") {
  translation(locale: $locale) {
    data {
      id
      attributes {
        entry(pagination: { limit: 100}) {
          ...Entry
        }
      }
    }
  }
}
